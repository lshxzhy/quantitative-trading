# Copy this file to config/datasets.yml and edit it after you obtain
# the official CSMAR API documentation from your school or CSMAR.

datasets:
  - name: example_stock_daily
    enabled: false
    description: "Example only. Replace endpoint/params with official CSMAR API settings."
    method: GET
    endpoint: "/replace-with-official-endpoint"
    params:
      start_date: "{last_success_date}"
      end_date: "{today}"
    output: "example_stock_daily_{today}.csv"

